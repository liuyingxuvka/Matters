## 1. G0 Source-Universe Scope, Privacy, and Toolchain Repair

- [x] 1.1 Create the `matters/` repository with external sibling `MATTERS_HOME` and `MATTERS_EVAL_VAULT` roots
- [x] 1.2 Initialize the repo-local OpenSpec change and connect FlowGuard 0.58.2 / Schema 1.0
- [x] 1.3 Refreeze the v0.2 source universe around authorized user-content sources, progressive per-item terminal disposition, Gmail read-only coverage, hard exclusions, and Jira/Rovo deferral; autonomy/UI/visual/desktop deltas remain open below
- [x] 1.4 Mark prior bounded-input, Jira-shaped, G0, G1, G8, L1-L10, and Gmail-shortlist evidence stale because the source universe, privacy boundary, and acceptance path changed
- [x] 1.5 Correct the FlowGuard project-audit disposition so `suite_inventory_unresolved` remains visible and cannot be called repaired
- [x] 1.6 Fix the `blobs/` ignore and public-policy collision so every required product source is admitted to Git and packaging
- [x] 1.7 Build one authoritative required-public inventory and compare it with ignored, tracked, package, and clean-clone inventories
- [x] 1.8 Replace machine-local paths, usernames, interpreter paths, host data, and private-root values in public evidence with portable identifiers
- [x] 1.9 Extend public scanning to decoded JSON/YAML, escaped Windows paths, POSIX homes, usernames, symlinks, junctions, and encoded path variants
- [x] 1.10 Exclude caches, bytecode, logs, ignored files, private roots, and runtime outputs from source fingerprints
- [x] 1.11 Add same-class privacy regressions for escaped paths, required ignored source, and cache-contaminated fingerprints
- [x] 1.12 Strictly validate the autonomy-revised OpenSpec change, including the ResearchGuard integration gate, 11-skill bundle, object browser, representative visual, coverage ledger, and desktop worker before model or production edits
- [x] 1.13 Freeze the authoritative user-content root, Gmail category/query, `cloud_placeholder`, attachment, archive, audio/video, database/model, link/junction, and `hard_excluded` policies
- [x] 1.14 Freeze the snake_case automatic classification/disposition vocabulary and prove complete coverage requires every enumerated item plus every required page/cursor to be terminal without a normal review state
- [x] 1.15 Prove `MATTERS_HOME` and `MATTERS_EVAL_VAULT` resolve outside Git/package/build roots and record their ACL, encryption, and cloud-sync dispositions
- [x] 1.16 Freeze the candidate-root manifest, tracking-disposition vocabulary, automatic AI triage thresholds, conservative protected-source rules, optional user-override rules, and changed-item dependency policy
- [x] 1.17 Produce a portable current ResearchGuard receipt with `checked_at`, source commit, distribution/command/top-level skill/member/manifest/residual state, compatibility, native validation, installed currentness, and terminal disposition without machine-local paths

## 2. G1 Plane-Partitioned Behavior Commitment Authority

- [x] 2.1 Rebase the ledger metadata, current revision, OpenSpec source references, and evidence ids on the revised v0.2 scope
- [x] 2.2 Update end-to-end product commitments for source-universe grants, staged local/Gmail discovery, automatic per-item disposition, durable state, optional correction, object coverage, visual selection, and desktop projection
- [x] 2.3 Add stable `agent_operation` commitments for text/multimodal AI, OCR, the abstract `ResearchOperation`, the single ResearchGuard provider, pending-integration, and terminal receipts; retire separate legacy Guard bindings
- [x] 2.4 Update the C10 commitment so original-owner execution and terminal dispositions are required before projection refresh
- [x] 2.5 Update the C12 commitment so pending or stale recomputation cannot be shown as a fresh canonical projection
- [x] 2.6 Retire the old release-blocking Jira-slice process commitment with an explicit replacement relation
- [x] 2.7 Add source-type synthetic, real-private canary, progressive first-run, and complete terminal-coverage process commitments while keeping Jira activation post-v0.2
- [x] 2.8 Split structural, current-runtime, and release ledger claim scopes so later not-run gates do not invalidate structural G1
- [x] 2.9 Bind every UI, HTTP, MCP, CLI, adapter, installer, and documentation surface to one stable intent and one primary path
- [x] 2.10 Run canonical plane-first lookup and bidirectional source/owner/path/freshness review
- [x] 2.11 Resolve every duplicate, missing owner, stale evidence, unknown relation, and primary-path finding required for the current gate
- [x] 2.12 Freeze the current G1 structural ledger fingerprint and keep later runtime/release evidence visibly not run
- [x] 2.13 Separate read/analyze authorization from Gmail mutations, file deletion/execution, external messaging, remote model disclosure, and GitHub publication commitments
- [x] 2.14 Add stable `development_process` commitments for bundled Skill Pack inventory, compatible active-view resolution, Matters-managed synchronization, rollback, ResearchGuard integration freeze, and self-maintenance Model Miss handoff

## 3. G2 Existing-Model Reuse and Child-Model Repair

- [x] 3.1 Run full Existing Model Preflight and freeze the reuse decision for M0 and C1-C12
- [x] 3.2 Extend C1 for source-universe and narrow grants, root/Gmail pagination, terminal coverage, hard exclusions, link containment, placeholders, revocation, and read-time change hazards
- [x] 3.3 Extend C2 for immutable local/document/image/mail/attachment/cloud occurrences, content versus metadata identity, parent-child provenance, idempotency, durable recovery, and tombstones
- [x] 3.4 Extend C3 for line/character, page/passage, slide/shape, sheet/cell, message/thread, attachment, image/OCR-region, metadata anchors, unsupported gaps, and stale anchors
- [x] 3.5 Revalidate C4 same-name and AI-proposed identity hazards without changing canonical ownership
- [x] 3.6 Revalidate C5 metadata-time versus event-time and advisory temporal-order hazards
- [x] 3.7 Generalize C6 admission from Jira-shaped input to authorized provider-neutral local sources and advisory candidates
- [x] 3.8 Generalize C7 provider/Jira state conflicts to source-label and AI-proposal conflicts
- [x] 3.9 Revalidate C8 anchored waiting target, closure condition, partial blocking, and advisory-gap behavior
- [x] 3.10 Generalize C9 completion/reopen signals from Jira/provider records to source revisions and AI proposals
- [x] 3.11 Extend C10 for durable invalidation, dependency-graph dispatch, every original-owner terminal disposition, retry, and restart
- [x] 3.12 Extend C11 for minimized text/multimodal work packages, runner/tool/provider identity, prompt-injection isolation, terminal statuses, schema validation, freshness, and no silent heuristic substitution
- [x] 3.13 Extend C12 for source catalog, staged progress, per-item gaps, user-on-demand evidence, pending recomputation, privacy status, English-default locale selection, required `en`/`zh-CN` localized-value storage, extensible locale registration, and current multilingual projection publication
- [x] 3.14 Extend M0 for source-universe coverage, local/Gmail/document/image analysis, agent-operation terminals, correction joins, blocked owner results, and fresh projection commitment
- [x] 3.15 Add one known-good and one known-bad per new protected failure and prove the intended oracle
- [x] 3.16 Complete template reuse/no-match and harvest disposition for every materially deepened model
- [x] 3.17 Run all 13 model owners under the current toolchain and freeze terminal evidence
- [x] 3.18 Add nested C1 discovery, C2 registry, C3 extractor/anchor, C10 recomputation, C11 runner, and C12 catalog/progress leaves without creating a duplicate top-level owner
- [x] 3.19 Extend C1/C2/C11/C12 for candidate-scope freeze, `tracked`/`not_tracked`/`hard_excluded`/`metadata_only`/`blocked`/`unavailable` dispositions, automatic AI triage, inventory snapshots, added/modified/moved/deleted/newly-reachable change sets, freshness invalidation, optional user override, and incremental reprocessing
- [x] 3.20 Build the separate auxiliary `S0_matters_skill_runtime` model with S1 inventory, S2 compatibility, S3 active resolution, S4 Matters-managed synchronization, and S5 validation/rollback, proving it is not C13 and owns no canonical Matter fields
- [x] 3.21 Model real ResearchGuard activation as an external currentness gate while preserving deterministic synthetic `ResearchOperation` execution and visible `researchguard_pending_integration`
- [x] 3.22 Replace every normal `review_required`/confirm/reject/defer transition in M0 and C1/C4-C12 with automatic owner routing, uncertainty-preserved terminal decisions, optional later correction, retry, and genuine blocked states
- [x] 3.23 Extend C2/C3/C6/C10/C11/C12 for visual derivatives, exact visual anchors, Matter-source relations, allowlisted AI selection, deterministic fallback, user pinning, stale invalidation, and neutral placeholders
- [x] 3.24 Extend C12 and UI Flow for the Databank-derived object browser, Standard/Compact density invariant, left filters, Matter cards/details, English/Chinese projection, desktop launch, and app-open autonomous worker
- [x] 3.25 Model `ObjectCoverageLedger` as a coordinator over existing owners, with occurrence/Matter stage completeness, retry, restart, UI-reachability, and no duplicate canonical ownership

## 4. G3 ModelMesh, Field Lifecycle, and Reattachment

- [x] 4.1 Update the parent partition and unique state/field/side-effect owner maps for source grants, discovery, dispositions, extraction, persistence, agent operations, and recomputation
- [x] 4.2 Inventory every new config, payload, persisted, receipt, API, and UI field in FieldLifecycleMesh
- [x] 4.3 Project behavior-bearing fields to their C1-C12 owners and give every old Jira-gate field or path a disposition
- [x] 4.4 Derive C10 dependency closure from registered graph edges rather than a hard-coded owner list
- [x] 4.5 Update ChildReattachmentContracts for every changed child and review affected siblings
- [x] 4.6 Update the mesh closure model for agent-operation results, recomputation owner joins, blocked terminals, and C12 publication
- [x] 4.7 Regenerate portable refinement bindings and parent-consumed current child/runtime evidence ids
- [x] 4.8 Run ModelMesh, field lifecycle, layered parent coverage, child disjointness, reattachment, and closure checks
- [x] 4.9 Freeze a current `mesh_green` receipt or keep exact remaining blockers visible
- [x] 4.10 Prove local/Gmail discovery leaves cannot write C3-C12 canonical state and every cross-source relation re-enters through current SourceVersion/EvidenceAnchor contracts
- [x] 4.11 Add the S0-S5 auxiliary skill-runtime mesh, active-view and managed-install fields, external ResearchGuard compatibility dependency, rollback edges, and explicit zero ownership overlap with M0/C1-C12

## 5. G4 Model-Derived Code and Test Design

- [x] 5.1 Regenerate the FunctionBlock-to-module map and public facade plan from the revised models
- [x] 5.2 Regenerate field reader/writer, durable state, side-effect, filesystem, Gmail, `cloud_placeholder`, document/image extractor, database, blob, runner, API, MCP, CLI, and UI ownership maps
- [x] 5.3 Update all affected owner CodeContracts and leaf boundary contracts
- [x] 5.4 Add SourceUniverseGrant, DiscoveryCursor/Page, SourceDisposition, extractor result, AnalysisWorkPackage, AgentOperationResult, durable repository, recomputation, API payload, and UI state contracts
- [x] 5.5 Regenerate the TransitionCoverageMatrix including unavailable, failed, stale, pending, retry, restart, revoke, and delete paths
- [x] 5.6 Extend ContractExhaustion with scope escape, link/junction loops, cloud placeholders, change-during-read, archive bombs, prompt injection, OCR/EXIF uncertainty, mail duplicates, unanchored AI, non-terminal receipts, omitted owners, early projection, and private-public leakage families
- [x] 5.7 Regenerate the field lifecycle, architecture, ContractExhaustion, and code-structure reports
- [x] 5.8 Revise TM0 and child-suite ownership for local input, persistence, AI/skill, UI, install, and release evidence
- [x] 5.9 Move Jira J1-J10 out of the required v0.2 inventory while retaining the dormant generic adapter as a non-blocking source-only asset
- [x] 5.10 Define provider-neutral synthetic families for local text/code, PDF/Office/spreadsheets, images/OCR/EXIF, Gmail/thread/attachments, `cloud_placeholder`, `metadata_only` types, and exact privacy oracles
- [x] 5.11 Regenerate Model-Code-Test Alignment and resolve every orphan obligation, contract, target, suite, and owner
- [x] 5.12 Freeze the G4 design inventory before production implementation
- [x] 5.13 Partition routine, source-type integration, real-private canary, progressive first-run, UI, installed-projection, and release-only test owners in TestMesh
- [x] 5.14 Derive the three-layer bundled-pack/machine-installed-inventory/resolved-active-view contracts, PEP 440 compatibility/prerelease rules, ResearchGuard adapter, managed-subtype synchronization, rollback, currentness invalidation, and no-global-install behavior from S0-S5
- [x] 5.15 Add exact bad families for same-version/different-hash, invalid or incompatible newer local skill, unmanaged lower local skill, absent local skill, failed activation rollback, author-control residuals, three-Guard parallel fallback, and incomplete ResearchGuard installation
- [x] 5.16 Create and validate a bidirectional OpenSpec traceability artifact mapping every capability requirement and scenario to implementation task, owner contract, TestMesh case, and current evidence disposition

## 6. G5 Durable Private Catalog and Source Adapters

- [x] 6.1 Implement explicit pasted-text registration with bounded authorization and idempotency
- [x] 6.2 Implement source-universe and narrow grants with declared roots, operations, exclusions, policy revision, revocation, and external-action separation
- [x] 6.3 Implement paged, checkpointed filesystem discovery with path containment, symlink/junction/reparse cycle defense, permission gaps, `cloud_placeholder` detection, cancel, and restart
- [x] 6.4 Remove Jira-shaped assumptions from the provider-neutral orchestration path without deleting the dormant adapter
- [x] 6.5 Implement `MATTERS_HOME` configuration with visible non-writing behavior when absent
- [x] 6.6 Implement SQLite schema and repository ownership for grants, discovery pages/cursors, occurrences, dispositions, sources, versions, evidence, advisory results, decisions, revisions, dependencies, work, and projections
- [x] 6.7 Integrate BlobStore transactionally for immutable source bodies and verify all blob references
- [x] 6.8 Integrate durable repositories into `MatterService` without creating a second in-memory business success path
- [x] 6.9 Implement stable local occurrence/content/metadata identity, read-time change detection, deduplication, rename/supersession, revocation, deletion, and tombstone behavior
- [x] 6.10 Implement deterministic line/character extraction for text, Markdown, code, and policy-approved configuration files
- [x] 6.11 Implement bounded PDF and Office page/passage/slide extraction with current extractor identity and visible unsupported/encrypted/corrupt outcomes
- [x] 6.12 Implement bounded spreadsheet sheet/cell/range extraction without formula execution or hidden external-data activation
- [x] 6.13 Implement photo/image metadata, OCR, and pixel-region extraction while separating EXIF, OCR, visual inference, and identity uncertainty
- [x] 6.14 Implement read-only paged Gmail message/thread/attachment discovery with Inbox/Sent/archived inclusion, Spam/Trash policy, durable cursors, and zero mailbox mutation
- [x] 6.15 Implement parent-child provenance for document parts, mail threads/messages/attachments, embedded images, and bounded archive members
- [x] 6.16 Implement `metadata_only`, `cloud_placeholder`, `quarantined`, `unsupported`, `inaccessible`, `changed_during_read`, `blocked`, `revoked`, and `deleted` dispositions with explicit terminal/retry classification
- [x] 6.17 Implement resource budgets, archive/decompression limits, unsafe-model/database quarantine, bounded concurrency, backpressure, cancel, and disk quota behavior
- [x] 6.18 Add catalog/store/discovery/extractor boundary tests for every declared good/bad cell and restart point
- [x] 6.19 Implement durable candidate-root and mailbox-policy manifests plus versioned tracking policies
- [x] 6.20 Implement metadata-first automatic classification into `tracked`, `not_tracked`, `hard_excluded`, `metadata_only`, `blocked`, `unavailable`, and the exact declared terminal dispositions
- [x] 6.21 Implement bounded AI-autonomous relevance/junk triage with confidence, alternatives, uncertainty codes, reason, policy/freshness identity, conservative protected-source policy, append-only optional user override, and zero authorization expansion
- [x] 6.22 Implement durable inventory snapshots and added/modified/moved/deleted/unchanged/newly-reachable change sets for folders and Gmail
- [x] 6.23 Invalidate stale classification/extraction/analysis by exact dependency and incrementally reprocess changed occurrences without duplicate no-change work
- [x] 6.24 Implement background scan scheduling, pause/cancel/resume, bounded queues, single-owner writes, checkpoint recovery, and UI-visible freshness without using unattended final verification
- [x] 6.25 Implement durable recursive filesystem partition manifests for roots that exceed the per-scope entry budget; inventory parent direct entries once, delegate safe child directories without following links or exclusions, resume pending children, and keep aggregate coverage partial until every required partition and occurrence is current and terminal
- [x] 6.26 Maintain a private per-occurrence source catalog and stable revision-bound page-token index so large inventories support bounded current source/object/history pages without serializing every private row
- [x] 6.27 Preserve original-root policy context across recursive child scopes, reclassify existing private inventory under a versioned reversible clutter policy, and retain explicit user overrides across policy-only reconciliation
- [x] 6.28 Implement the durable ObjectCoverageLedger schema, repository, per-stage dependency fingerprints, bounded summary API, restart recovery, and exact complete/partial/blocked reconciliation
- [x] 6.29 Implement persistent C4-C9 owner repositories and startup hydration so automatic decisions and recomputation survive restart without generic `understanding_owner_output` substitutes
- [x] 6.30 Implement safe private visual derivatives for images and supported documents with renderer identity, macros/network/external-content disabled, exact parent anchors, resource budgets, and invalidation

## 7. G6 AI/ResearchGuard Operations, Self-Maintenance, and Original-Owner Recompute

- [x] 7.1 Implement versioned AnalysisWorkPackage and AgentOperationResult schemas
- [x] 7.2 Implement injected text, multimodal/OCR, local-skill, and abstract ResearchOperation runner boundaries with explicit pending-integration, unavailable, failed, partial, quarantined, scope-incompatible, and current terminal statuses
- [x] 7.3 Implement schema, authorization, minimized-payload, scope, input-fingerprint, model/provider/tool/skill identity, resource, freshness, and evidence-anchor validation
- [x] 7.4 Remove keyword heuristics and manual confirmation from the AI-equivalent success path while keeping optional correction explicitly distinct
- [x] 7.5 Implement advisory candidate ingestion for ResearchGuard and AI results without canonical writes; classify legacy separate Guard outputs as stale/source-only migration evidence
- [x] 7.6 Implement durable automatic original-owner dispatch plus `proposed`/`auto_applied`/`policy_rejected`/`no_finding`/`superseded`/`corrected_by_user`/`blocked` finding history
- [x] 7.7 Implement durable invalidation plans and dependency-graph recomputation dispatch
- [x] 7.8 Implement actual C2-C11 original-owner execution or explicit terminal blocked dispositions in dependency order
- [x] 7.9 Make C12 wait for the current M0 recomputation join before publishing a canonical projection
- [x] 7.10 Implement idempotent recomputation retry and restart recovery without duplicate revisions or projections
- [x] 7.11 Add observed-regression and same-class tests for request-only correction, omitted owner, failed owner, repeated dispatch, and early projection
- [x] 7.12 Reattach C10, affected siblings, M0, and C12 with current runtime evidence
- [x] 7.13 Isolate prompt-like source content from runner control instructions and deny undeclared tool calls, adjacent-source reads, external actions, or public transfer
- [x] 7.14 Record private disclosure/minimization dispositions for any declared external model call without placing payloads, paths, hashes, excerpts, or embeddings in public receipts
- [x] 7.15 Implement the deterministic synthetic ResearchOperation runner and exact contract tests before any real ResearchGuard dependency
- [x] 7.16 Implement `not_assessed`/`partial`/`sufficient`/`blocked`/`stale` semantic depth, exact sufficiency criteria, dependency-bound freshness invalidation, missing-model-work queues, optional correction feedback, and bounded self-maintenance through existing C1/C2/C10/C11/C12 owners
- [x] 7.17 Convert software behavior/model misses into explicit development work items and prohibit live edits to production code, OpenSpec, FlowGuard models, validators, or canonical rules
- [x] 7.18 Integrate the real provider only after one frozen ResearchGuard package, top-level skill, member projections, manifest, compatibility declaration, native validation, and installed-currentness identity pass; never install or retire its ecosystem from the Matters worktree
- [x] 7.19 Upgrade bounded private semantic-understanding work packages to v2 prompt/schema/policy/owner/locale identities, exact input disposition accounting, bilingual `en`/`zh-CN` findings, and automatic original-owner dispatch without confirmation
- [x] 7.20 Implement a persistent idempotent dispatcher/outbox keyed by finding/source/owner/model identities with exact retry budget, terminal owner result, restart recovery, and C12 join
- [x] 7.21 Implement typed owner schemas for Matter, Person, Event/deadline, OpenLoop, lifecycle/outcome conflict, completion gap, bounded summary, and card visual without arbitrary attribute dictionaries
- [x] 7.22 Implement background autonomous-maintenance scheduling that repeatedly advances missing/stale coverage-ledger stages while the app runs and shuts down cleanly without owning final verification
- [x] 7.23 Remove the old understanding action-token, confirm, reject, defer, review queue, and compatibility success paths from service, CLI, HTTP, MCP, UI, skills, and tests

## 8. G7 Runnable CLI, HTTP, MCP, Codex Skill, and UI

- [x] 8.1 Define one MatterService facade for health/capability, source grants, discovery/extraction progress, dispositions, autonomous analysis, list, detail, correct, revoke, delete, visual intents, status, coverage audit, and projection
- [x] 8.2 Implement a local CLI with explicit home, source-scope preview, inventory/canary/expand/pause/resume commands, pasted/file input, analysis-package export/import, serve, status, and version
- [x] 8.3 Implement HTTP JSON endpoints for every supported service capability with bounded errors and no second business path
- [x] 8.4 Implement MCP methods matching the supported service capabilities and remove manifest overclaims
- [x] 8.5 Replace the app-local Matters consumer Skill Pack with exactly eleven required skills: source governance, inventory reconciliation, freshness, model depth, human correction, model-miss review, skill runtime, single ResearchGuard orchestration, semantic understanding, autonomous maintenance, and card-visual curation
- [x] 8.6 Scaffold a valid local Codex plugin projection only if the installed runtime contract can be validated; otherwise install package and skill as distinct domains
- [x] 8.7 Model and implement UI launch, source catalog, capability, control, event, state, recovery, keyboard, evidence-detail, and privacy interactions
- [x] 8.7a Keep source, Matter, and history transport/DOM pages bounded, provide accessible next/previous navigation, preserve locale/revision/focus, and reject stale page or disposition tokens
- [x] 8.7b Bound CLI, HTTP, MCP, and DOM source/object/history reads while keeping every row reachable and preserving revision, locale, task, and focus
- [x] 8.8 Implement source-universe/narrow-grant preview, hard exclusions, Gmail read-only boundary, pasted input, local roots, and exact file selection
- [x] 8.9 Implement analysis progress, unavailable, failed, partial, retry, and success states
- [x] 8.10 Implement candidate/source-only/admitted/blocked lists and Matter evidence detail
- [x] 8.11 Remove normal confirm/reject/defer controls and implement optional correction, cover override, revocation, deletion, recomputation-pending, exception, and recovery controls
- [x] 8.12 Implement a shared locale registry with default `en`, required `en`/`zh-CN`, BCP 47 ids, local preference persistence, and future-locale registration
- [x] 8.12a Persist every user-visible AI-authored value as validated locale-keyed text on one semantic revision and block missing, stale, conflicting, or unregistered locale publication without silent fallback
- [x] 8.12b Implement English-default/`zh-CN` projection switching across UI, CLI, HTTP, and MCP while preserving task state, evidence selection, and focus context
- [x] 8.12c Move every visible static label, action, empty/loading/error/recovery state, and accessibility string into the required `en`/`zh-CN` catalog
- [x] 8.13 Keep machine-local paths, raw receipts, internal ids, and debug metadata off the ordinary UI
- [x] 8.14 Run browser DOM, click-through, keyboard/focus, failure/recovery, on-demand evidence, English-default launch, persisted Chinese selection, missing-locale blocking, and bilingual equivalence validation
- [x] 8.15 Package the UI, CLI, MCP facade, immutable consumer Skill Pack, matching runtime assets, schemas, manifests, native validators, and synthetic fixtures without author-side SkillGuard control state
- [x] 8.16 Implement durable metadata-inventory, canary, expansion, pause/cancel, resume, terminal-disposition, partial-coverage, and completion progress views
- [x] 8.17 Show external-model disclosure/minimization status on demand while keeping internal paths, message identifiers, receipts, and private payloads hidden
- [x] 8.18 Implement bundled/machine-installed/active-view inventory and compatibility-aware singular resolution for every required skill
- [x] 8.19 Implement no-global-install bundled use, newer-compatible-local overlay, newer-bundled internal use, update-available for unmanaged installs, and visible same-version hash/incompatibility blocks
- [x] 8.20 Implement transactional synchronization and rollback only for the Matters-managed subtype inside the machine-installed layer, with separate package, bundled-layer, machine-installed-inventory, resolved-active-view, and ResearchGuard identities
- [x] 8.21 Expose ResearchGuard pending/current/blocked status and skill resolver decisions in capability and on-demand UI without displaying internal paths or receipts
- [x] 8.22 Remove source-level `track`, `do_not_track`, `restore`, and correction controls from the ordinary first-version UI while retaining automatic C1 policy, append-only C10 correction capability, and affected-work feedback outside the primary object-browser path
- [x] 8.23 Replace the old understanding-review workspace with the Databank-derived Matter object browser, Standard/Compact density, left filters, cards, detail dialog, on-demand evidence/history, stale-response rejection, request cancellation, and focus recovery
- [x] 8.24 Update exact bundle-declared machine-skill discovery, verified manifests, compatible overlays, collision reporting, and transactional Matters-managed sync for the revised eleven-skill inventory
- [x] 8.25 Freeze the Databank handoff hash and implement reusable Shell, Sidebar, filter, Standard/Compact Card, hero, status, detail rail, timeline, evidence, loading, empty, stale, blocked, and error components without changing its visual language
- [x] 8.25a Implement the fixed Matter-card projection: hero or placeholder, primary status, localized title and one-line summary, last-or-next significant time, and key people in both densities, plus Standard-only event, people, and source counts
- [x] 8.25b Implement projection-only catalog search with stale-response rejection and exact restoration of pre-search density, filters, locale, selected Matter, focus, and scroll without a canonical write, modeling request, or confirmation
- [x] 8.26 Implement Matter catalog/detail/search/filter/timeline/people/open-loop/related/source/history APIs and opaque private thumbnail endpoints through the shared MatterService
- [x] 8.26a Implement localized human-readable timeline sentences with distinct source-record and claimed/planned/observed occurrence times, explicit planned/reported/observed/inferred modality, and current-best interpretation plus preserved conflicts and alternatives
- [x] 8.27 Direct-replace the old real-image representative-visual route with one automatic generated-hero disposition per root and independently openable child Matter, shared across Standard/Compact/detail but kept separate from the real Images evidence gallery, with typed temporary placeholders and no ordinary `Change cover`, pin, real-image fallback, or second authority
- [ ] 8.28 Implement Figma component/library and 1880x900/1440x900 Standard/Compact English/Chinese design frames from the same handoff authority, compare captures, and keep real private visuals out of design artifacts — externally blocked because the connected Figma Starter MCP plan has exhausted its tool quota; runtime UI evidence remains independent and current
- [x] 8.29 Implement a Windows desktop wrapper that starts the local service and durable worker, opens the packaged object browser, persists locale/density/window state, and supports clean shutdown/restart
- [x] 8.30 Fix the single-card vertical-stretch defect while preserving the handoff's standard/compact geometry and detail-dialog behavior
- [x] 8.31 Package desktop assets and verify browser-development and desktop-production surfaces delegate to the same MatterService path
- [x] 8.32 Install the user-approved red/yellow/blue Matters icon with a transparent external canvas as the canonical UI brand, browser-window, packaged-application, and Windows-shortcut icon; render the stacked cards without an additional white container and enlarge only the left-sidebar icon and adjacent `Matters` wordmark under the recorded bounded presentation exception

## 9. G8 Fully Synthetic Source-Universe Safety and End-to-End

- [x] 9.1 Run synthetic pasted text and local text/code/config through grant, catalog, SourceVersion, anchors, agent work, admission, lifecycle, and projection
- [x] 9.2 Run synthetic PDF/Office/spreadsheet, image/OCR/EXIF, Gmail/thread/attachment, `cloud_placeholder`, and `metadata_only` occurrences through the same real service path
- [x] 9.3 Run unavailable, failed, stale, unanchored, and scope-incompatible agent operations
- [x] 9.4 Run valid, uncertain, unsupported, invalid, retried, and restarted automatic findings through exactly one declared canonical owner without confirmation
- [x] 9.5 Run correction through durable invalidation, every original-owner terminal result, M0 join, and fresh C12 projection
- [x] 9.6 Run restart recovery during source registration, analysis, and recomputation
- [x] 9.7 Prove page/cursor retry idempotency, partial coverage, per-item terminal disposition, no-delta, `revoked`, `deleted`, `blocked`, `unsupported`, `cloud_placeholder`, `quarantined`, and `changed_during_read` behavior
- [x] 9.8 Run conformance replay, leaf boundary matrices, Model-Code-Test Alignment, and TM0 routine suites
- [x] 9.9 Run the portable public-boundary scan over all generated public evidence
- [x] 9.10 Run scope-escape, link/junction loop, credential exclusion, archive bomb, unsafe model, prompt injection, OCR uncertainty, EXIF overclaim, quoted-mail duplicate, and private-public leak families
- [x] 9.11 Freeze the current synthetic source-universe evidence set
- [x] 9.12 Run deterministic ResearchOperation cases for pending ResearchGuard, current fake provider, invalid output, legacy parallel binding rejection, typed findings, and evidence-bound temporal/source/argument results
- [x] 9.13 Run the full S0-S5 skill-runtime matrix including absent local, exact match, compatible newer local, bundled newer managed/unmanaged, hash collision, invalid manifest, native validation failure, rollback, and restart
- [x] 9.14 Prove the packaged Skill Pack runs internally on a clean machine profile without creating a global skill installation
- [x] 9.15 Run autonomous workflow tests for input accounting, prompt injection, no-finding, uncertainty-preserved owner decisions, invalid output, retry, restart, no review route, early projection rejection, and optional correction
- [x] 9.16 Run representative-visual tests for allowlist enforcement, safe rendering, stable ties, AI failure, placeholders, user pinning, deletion/revocation, private thumbnail access, and Standard/Compact identity
- [x] 9.17 Run desktop/UI matrix at 1880/1440/1280/1180/1024/760/390 widths, 0/1/2/3/6/7/8/9/28/many cards, Standard/Compact, en/zh-CN, keyboard/focus/scroll/search/detail, and all visual states
- [x] 9.17a Run exact bilingual UI regressions for fixed card fields and Standard-only metrics, search-state restoration and stale-response rejection, and timeline record/occurrence-time, modality, current-best, conflict, and alternative rendering

## 10. G9 Local-First AI-Assisted L1-L10 Slices

- [x] 10.1 L1: paste a clear obligation and preserve exact evidence anchors
- [x] 10.2 L2: select one Markdown/TXT source without reading siblings
- [x] 10.3 L3: preserve two conflicting date claims, automatically select the best current interpretation, and retain conflict/alternatives without user confirmation
- [x] 10.4 L4: keep two same-name people separate without identity evidence
- [x] 10.5 L5: identify a waiting object, target, and closure condition
- [x] 10.6 L6: distinguish planned time, record time, and occurred time
- [x] 10.7 L7: reject `Done`, `final`, or AI completion language as sole completion proof
- [x] 10.8 L8: persist and project one semantic revision equivalently in required English and Chinese values, launch in English by default, switch to Chinese without state loss, and reject missing-locale publication
- [x] 10.9 L9: complete user correction, invalidation, all-owner recomputation, and fresh projection
- [x] 10.10 L10: terminate revocation, deletion, unreadable input, or skill failure as partial/blocked without data leakage
- [x] 10.11 Produce generic SliceCoverageReceipt and MatterDepthReport artifacts without Jira-required fields
- [x] 10.12 Convert every discovered model miss into an observed and same-class fully synthetic regression
- [x] 10.13 Obtain current ModelMesh, alignment, TestMesh, UI, privacy, and DPF evidence for the completed slice set
- [x] 10.14 Produce per-occurrence semantic-depth, adoption, stale-dependency, missing-owner-stage, visual, localization, UI-reachability, and terminal-coverage reports that reconcile exactly to the inventory snapshot
- [x] 10.15 Prove a no-change rescan produces no duplicate analysis and added/modified/moved/deleted/policy-change cases schedule only declared affected work
- [x] 10.16 Backpropagate the real Gmail canary miss so bounded content selection prioritizes supplied authorized bodies, preserves the exact limit, and has observed plus same-class synthetic regressions
- [x] 10.17 Mark the prior hierarchy-free C5-C12/M0, UI, coverage, package, install, and release evidence stale for the new root/child Matter contract while preserving historical receipts
- [x] 10.18 Extend OpenSpec traceability for stable semantic Matter identity, single-primary-parent acyclic containment, WorkItem/Event boundaries, hierarchy stages, and single-dialog recursive detail
- [x] 10.19 Extend C5/C6/C7/C8/C9/C10/C11/C12/M0 and the existing ModelMesh without adding a top-level C13; add good/bad hierarchy scenarios and current child reattachment
- [x] 10.20 Extend FieldLifecycleMesh, CodeContracts, TransitionCoverage, ContractExhaustion, Model-Test Alignment, UI Flow Structure, and TestMesh for every new hierarchy, path, freshness, and detail-reader field
- [x] 10.21 Direct-migrate current Matters as roots with stable semantic identities, append-only containment revisions, ancestor indexes, and no automatic conversion of old Related Matter candidates
- [x] 10.22 Implement WorkItem/Action ownership, Event assignment, required/optional/critical child roles, and acyclic single-primary-parent enforcement
- [x] 10.23 Implement independent child lifecycle/outcome plus evidence-bound parent state, completion, blocking, narrative summary, and descendant-milestone projection
- [x] 10.24 Implement reparent/split/merge/correction invalidation for old and new ancestor chains with restart-safe original-owner recomputation
- [x] 10.25 Add hierarchy coverage stages and one bounded indexed audit that reports the first missing/stale stage from registration through UI reachability without full-ledger deserialization
- [x] 10.26 Replace per-request full ObjectCoverageLedger materialization with paged/indexed summary, catalog, relation, detail, and coverage queries and prove large private inventories remain responsive
- [x] 10.27 Wire one real bounded model-agnostic Codex runner for semantic understanding and original-owner dispatch while preserving typed unavailable/pending/blocked outcomes and private disclosure limits
- [x] 10.27a Define and implement capability-role contracts plus a replaceable private Codex execution profile; use `gpt-5.6-luna` low reasoning only as the current local `low_cost_annotator` mapping, never as a product or release dependency
- [x] 10.27b Manually rehearse and then install one Codex-hosted daily maintenance schedule that reuses the bounded service path, resumes durable work, records private receipts, performs no source/mailbox/outbound mutation, and never owns unattended final model/full-test/install/Git/tag/release verification
- [x] 10.28 Restore the frozen Databank typography, color, spacing, shell, and Standard/Compact card geometry while preserving the user-approved larger Matters logo and adjacent title; remove other unapproved image overlays or one-off font changes
- [x] 10.29 Replace flat/duplicate navigation with one All matters entry plus collapsible Status, Start time, People, Relationships, Topic/Type, and Source type groups
- [x] 10.30 Implement root-only catalog, child-aware localized search paths, grouped 50-row Sub-matters table, one-dialog breadcrumb/back stack, focus/scroll restoration, descendant milestone timeline, exactly seven ordinary detail sections, the handoff-derived Images gallery, and the bounded Files & information table
- [x] 10.31 Keep loading, processing, ready, honest-empty, no-filter-results, ready-stale, and transport-error states distinct so failed fetches never render fake zero
- [x] 10.32 Extend the eleven bundled Matters skills and prompt/schema contracts so autonomous maintenance, semantic understanding, depth, correction, model-miss, and visual curation understand hierarchy and model-agnostic capability roles without owning canonical state or requiring a named model
- [x] 10.33 Run travel and job-search semantic families across 0/1/5/50/500 children, 1/2/5/20 depths, cycles, multiple parents, reparent/split/merge, bilingual text, model-profile substitution/escalation/unavailability, density parity, keyboard/focus, and 1880x900/1440x900 geometry
- [x] 10.34 Run Model Miss Review for the real zero-card/fetch-timeout episode, bind observed and same-class regressions to the responsible model/code/test owners, and close the generalized class
- [ ] 10.35 Rerun affected model owners and focused backend/UI tests, then freeze the new model, field, UI, alignment, and TestMesh inventories before private expansion
- [ ] 10.36 Recheck private/public boundaries after hierarchy summaries, breadcrumbs, thumbnails, search indexes, logs, screenshots, and receipts are generated
- [x] 10.37 Direct-replace stale filesystem partition manifests under the current deterministic policy; hard-block program/config/cache/internal/unknown machine files before AI, keep declared unsupported media/archive metadata-only, and preserve ordinary user documents
- [x] 10.38 Keep filesystem neighborhood/group context stable across partition boundaries, pass only bounded private spatial context to AI, and retire deleted or policy-pruned scopes/objects from active coverage, Matter reachability, and UI counts with restart/reactivation tests
- [x] 10.39 Direct-migrate retired AI capability roles and exclude inactive or current non-tracked source packages from runnable work so policy hard exclusions also terminate historical program-file AI tasks
- [x] 10.40 Implement atomic evidence-bound containment batches with one summary publication, idempotent retry, pending-publication restart recovery, 500-child bound rejection, and a 200-child write-throughput gate
- [x] 10.41 Direct-migrate collapsed legacy C12 projections from exact current C6/C12 owner rows, forbid summary-as-title fallback, bind ownerless findings only to the unique same-result Matter admission, include title/summary in catalog freshness, and prove restart, owner-repair, ambiguity, missing-result, manual-title, and concurrent rebuild behavior
- [x] 10.42 Bound registered-file claims to small recoverable pages, release caught interruptions immediately with exact checkpoint preservation, shorten crash leases, and split complete anchor registration from cursor-resumable AI package materialization so large documents cannot block the whole source queue or lose later evidence
- [x] 10.43 Replace the real multi-minute pending-analysis correlated history query with current owner-indexed bulk filtering, preserve capability/source/dependency/migration/result semantics, and prove a 2,500-package page remains bounded

## 10A. Latest-Decision Semantic Rebaseline: Material Clues, Generated Heroes, and Eight-Section UI

- [x] 10.44 Mark the prior real-image representative-visual, permanent-placeholder, user-pin/change-cover, exactly-seven-section, symmetric-sidebar-spacing, and pre-loop UI screenshot/model/test evidence stale while preserving it as history
- [x] 10.45 Update the existing BCL commitments and singular primary paths for Matter merge/hierarchy, material-clue summary/activity projection, generated-hero publication, AI supplemental information, Codex orchestration/delegation, and iterative UI evidence without creating a second runtime authority
- [x] 10.46 Update FieldLifecycleMesh for `MaterialClue`, `latest_meaningful_clue_at`, clue/summary revision, ancestor activity propagation, generated-hero brief/asset/status/alt/safety/currentness fields, AI supplemental-information freshness, and direct retirement of real/document hero selection, pin/unpin, change-cover, and permanent-placeholder success fields
- [ ] 10.47 Update M0, C5, C6, C10, C11, C12, AgentOperation, ObjectCoverageLedger, ModelMesh, CodeContracts, ContractExhaustion, Model-Test Alignment, TestMesh, UI Flow Structure, and DPF for the rebaseline; add current good/bad cases and reattach every affected parent/child evidence identity
- [x] 10.48 Implement context-aware Matter reconciliation using evidence-bound goal, subject, outcome, people, time, source-neighborhood, provider-thread, repository/project, and Codex-workspace context; prefer broad root Matters and route small records to children, WorkItems, Events, or sources without single-signal overmerge
- [x] 10.49 Implement `MaterialClue` admission, materiality decisions, child-to-ancestor activity propagation, atomic `en`/`zh-CN` summary refresh, stable `latest_meaningful_clue_at DESC` catalog queries after search/filter, and strict separation from Start time and processing time
- [x] 10.50 Add `maintenance_orchestrator` and `hero_image_generator` to the model-agnostic capability router; make the strongest compatible current profile plan/judge each run and delegate only typed bounded low-cost work to cheaper replaceable background profiles with private execution receipts and no application API key
- [x] 10.51 Direct-replace bundled `matters-card-visual-curation` with `matters-hero-image-generation` while retaining exactly eleven bundled skills, preserving one C12 publication authority, and updating manifests/prompts/schemas/service contracts/native checks under SkillGuard supervision
- [x] 10.52 Implement the private generated-hero work package and store under `MATTERS_HOME`: identity/hierarchy/theme stability gate, minimized text-free/logo-free/identifier-free/person-safe brief, one generated asset shared across locales, bilingual alt text, bounded retry, typed pending/blocked placeholder, semantic invalidation, and zero SourceVersion/Evidence authority
- [x] 10.53 Implement exactly eight ordinary Matter-detail sections including localized `AI Supplemental Information` / `AI 补充信息`, keep real photos and document-derived visuals only in the operable Images gallery, and keep supplemental background/rules/timezone/preparation items advisory, source-related, and freshness-bound
- [x] 10.54 Apply only the approved sidebar exception: preserve current logo/title size, move search plus navigation upward, keep brand-to-search and search-to-navigation gaps smaller than the top brand whitespace, and do not force equal upper/lower brand spacing
- [x] 10.55 Establish the iterative 8766 UI gate for every meaningful UI batch at 1880x900 and 1440x900 across en/zh-CN, Standard/Compact, long text, loaded/processing/empty/error/detail/filter/gallery/child states, with screenshot plus DOM geometry checks for text/image/metric overlap, clipping, duplicate dates/summaries/people, section overflow, and incoherent whitespace
- [x] 10.56 Add same-class regressions for real photo/mail/TXT/document screenshots entering hero, generated hero treated as evidence, normal clue causing unnecessary regeneration, material clue failing to refresh summary/order, non-material scan causing bubbling, Start time/clue/processing time confusion, density/locale/filter changing relative order, missing eighth section, and every declared overlap/repetition defect
- [x] 10.57 Run the conditional Guard-upgrade gate: minimize any unexpressible failure, prove the current owning Guard cannot represent/check it before changing that Guard, use its separate OpenSpec/native validation/SkillGuard supervision/install sync, replay the Matters case, and otherwise keep the repair entirely in Matters
- [ ] 10.58 Run affected model owners and focused runtime/UI/privacy tests after each owner boundary; freeze current model, field, UI, alignment, TestMesh, bundled-skill, prompt/schema, and private-asset inventories before real expansion
- [ ] 10.59 Update the Codex daily-maintenance automation only after manual rehearsal so its strongest primary task delegates cheap annotation/generated-hero batches through typed packages, remains model-agnostic, and never owns unattended final model/full-test/install/Git/tag/release verification
- [x] 10.60 Reconcile every new generated-hero, clue/summary, supplemental-information, and UI-reachability stage in the one-click coverage audit so every registered source and admitted root/child Matter reports its first missing/stale/blocked stage through actual UI display
- [x] 10.61 Close the post-green coverage Model Miss by synchronizing actual SemanticDepth and hierarchy-owner results into ObjectCoverageLedger stages, including invalidation and recomputation, while proving missing owner output remains pending instead of being written green
- [x] 10.62 Replace startup-only legacy package conversion with an explicit bounded, resumable, idempotent current-contract rebase that supersedes stale results, rebuilds dependencies in topological order, preserves historical snapshots, and can resume after interruption without one unbounded database transaction
- [x] 10.63 Replace the first-50 Matter-context shortcut with deterministic whole-catalog candidate recall plus a bounded top-ranked AI context so a matching Matter beyond row 50 remains discoverable without sending the whole private catalog to AI
- [x] 10.64 Expose supported private Codex handoffs for pending analysis, current-contract rebase, generated-Hero brief/import/failure, ResearchOperation request/result, and maintenance receipts without embedding an application API key or named product model
- [ ] 10.65 Record the owner/result coverage, stale-package, global-recall, Hero-handoff, ResearchOperation, and Codex-schedule failures as typed Model Miss rows; add same-class tests and rerun affected M0/C6/C11/C12, ModelMesh, alignment, TestMesh, DPF, privacy, and live UI evidence before restoring broad confidence
- [x] 10.66 Add `content_selection` between inventory and source version so full registration never implies an immediate full-content read; persist one private mode/reason/priority/neighborhood/freshness/continuation plan per active tracked occurrence
- [x] 10.67 Make deterministic hard exclusions terminal before AI, default safe software-tree support files and bulk images to metadata-only/deferred, and admit only explicitly sampled/bounded/resumable-deep plans to registered-content claims
- [x] 10.68 Order bounded selection and read claims by stable private value and neighborhood signals, preserve distinct Source identity, expose exact pending/deferred/selected/blocked coverage, and prove interruption/idempotent resume without object-id priority
- [x] 10.69 Prevent source/anchor/package amplification by queuing only plan-admitted anchors, keeping unseen content explicitly pending, and requiring a durable continuation before deep extraction; do not silently truncate the document adapter or delete existing history
- [ ] 10.70 Record the real 53,706-document / 15,934-image selection-gap and startup-unbounded-migration misses, add same-class scale/restart/privacy regressions, and rerun affected C1/C2/C3/C11/M0, ModelMesh, alignment, TestMesh, DPF, OpenSpec, and live coverage evidence before restoring broad confidence
- [x] 10.71 Remove private-catalog migration from foreground service construction; retain explicit bounded coverage, analysis-contract, and content-selection rebase owners plus a no-startup-mutation regression
- [x] 10.72 Rebase the coverage-stage contract only across active tracked occurrences with bounded continuation, add current `content_selection`, preserve retired/non-tracked history unchanged, and prove exact retry/restart behavior
- [x] 10.73 Deduplicate overlapping registered filesystem scopes by stable occurrence identity during content-selection rebase while preserving complete licensed neighborhood context and one current plan
- [x] 10.74 Reconcile active source coverage rows missing from current inventory through one bounded resumable orphan owner, preserve history, retire ghost work from active counts/queues/UI, and prove idempotent restart
- [x] 10.75 Make parent composition atomic across admission, projection, classification, coverage, containment, activity, and supplemental-information owners; prove an attachment failure leaves no parent residue
- [x] 10.76 Add one append-only `matter_canonicalization` owner for same-Matter merge, candidate append, and source-only retirement; atomically rewrite/remove coverage references, preserve evidence, hide superseded projections, and prove exact retry idempotence plus conflicting-retry rejection
- [x] 10.77 Correct future-due activity rows from exact historical SourceVersion or current-event observation time, preserve claimed/due time separately, propagate the correction to ancestors and late-bound canonical Matters, and prove missing observation time never falls back to processing time
- [x] 10.78 Replace full-projection/full-card catalog construction with indexed filter/order/page selection followed by visible-id hydration and bounded aggregate facets; add large-catalog query-count, order, locale/density parity, and no-full-materialization regressions
- [x] 10.79 Refresh C1/C2/C5/C6/C10/C12/M0, ModelMesh, inventory, FieldLifecycleMesh, TransitionCoverage, ContractExhaustion, Model-Test Alignment, TestMesh, DPF, and adoption evidence for 10.72-10.78; run strict OpenSpec validation and preserve any later gate blocker without treating bounded UI payloads as query-shape proof
- [x] 10.80 Replace latest-child-as-parent-summary projection with an evidence-bound bilingual parent narrative over the complete current child/projection/evidence revision set; keep title, lifecycle, activity recency/order, and generated-hero identity outside its write authority; requeue after any bound input changes
- [x] 10.81 Backpropagate the proven storage and identity Model Miss family into OpenSpec and current C1/C2/C3/C6/C12/M0, BCL, FieldLifecycle, ContractExhaustion, CodeContracts, ModelMesh, TestMesh, and DPF: inventory scan revision is contextual rather than content-selection semantic identity; downstream evidence uses bounded version/count/digest pointers; coverage keeps one full current payload plus exact compressed noncurrent history; canonical hierarchy/coverage/UI require the exact C6-admitted `matter_id`
- [ ] 10.82 Run the real private recovery only after all writers stop and a separate verified restorable copy exists: complete bounded evidence-pointer rebase pages before bounded coverage-history archive pages, verify integrity/count/digest/history samples and resumable cursors, preserve originals until archive verification, and perform no startup migration, `VACUUM`, `VACUUM INTO`, or physical-shrink claim
- [ ] 10.83 Rerun strict OpenSpec, affected M0/C1/C2/C3/C6/C12 hazard scenarios, ContractExhaustion, FieldLifecycle, CodeContracts/Model-Test Alignment, ModelMesh, TestMesh, DPF, and focused source/coverage/hierarchy/projection tests; keep private migration, installed-runtime, physical-compaction, full regression, release, and publication claims explicitly not run until their own current terminal receipts exist

## 10B. v0.3.0 Source-in-Place, Situation Graph, World Model, and Final Object Browser

- [x] 10.84 Direct-rebaseline OpenSpec and mark copied-original, durable Gmail-body, child-Hero, conceptual-Hero, card-summary, recursive-child-reader, Overview source/evidence/Action/OpenLoop panels, multi-type visible graph nodes, per-node collapse controls, duplicate logical timeline events, ungrouped-source-row, and inference-without-World-Model evidence stale without deleting history
- [ ] 10.85 Update BCL plus M0/C1/C2/C3/C5/C6/C7-C12, ModelMesh, FieldLifecycle, CodeContracts, ContractExhaustion, UI Flow Structure, Model-Test Alignment, TestMesh, DPF, and adoption evidence for source-in-place storage, private retention classes, SourceGroup, internal SituationGraphSnapshot plus Matter-only C12 hierarchy projection, logical-event identity/supersession, advisory Situation/World Model inference, root-only photographic Hero, summary-free cards, one reusable node-specific quick view, and compact truthful coverage status
- [x] 10.86 Implement the direct-current pointer/fingerprint SourceVersion authority, durable-derived schema, transient/cache/backup classes, reference accounting, bounded TTL/GC, source-unavailable currentness, and no normal-runtime copied-original fallback
- [x] 10.87 Implement and synthetically rehearse the offline bounded migration that freezes writers, verifies a separate restorable backup, replaces legacy text/Gmail/original-image copies, cleans connector/staging/orphan-cache residuals, verifies derived equivalence/integrity/counts, and keeps physical compaction separate; the user's real private-store migration remains owned by 11.12i
- [x] 10.88 Make filesystem and Gmail extraction consume complete content transiently, atomically persist only locator/fingerprint plus declared derived state, clean staging on success/terminal failure, and prove no source-directory/mailbox mutation
- [x] 10.89 Implement stable private SourceGroup identity, membership, freshness, bounded group/member queries, contained-label privacy, folder/Gmail-thread/Codex-project/provider grouping, and group coverage without exposing absolute paths or creating Matter identity
- [x] 10.90 Implement bounded SituationGraphSnapshot queries over C5 Events, C6 hierarchy/typed relations, WorkItems, and C11 inference nodes/edges with primary/secondary distinction, arbitrary-depth continuation, exact freshness fingerprints, and zero second canonical owner
- [x] 10.91 Implement persistent model-agnostic Situation/World Model inference with confirmed/reported/planned/ai_inferred modalities, past-only historical-gap inference, testable future prediction snapshots, verification/contradiction conditions, observation horizons, alternatives, weakening/coverage bounds, expiry, dependency invalidation, restart recovery, original-owner dispatch, and contradiction-triggered Model-Miss review without canonical backwrites
- [x] 10.92 Extend CLI/HTTP/MCP/MatterService and the one-click ObjectCoverageLedger audit with a materialized indexed first-gap view for source groups, graph pages, node quick view, World Model freshness, root-Hero versus descendant-not-applicable stages, raw/staging cleanup, inventory freshness, semantic/hierarchy/summary consistency, and actual UI reachability; make bounded pages return in seconds at the full private-ledger scale and prevent terminal hard exclusions from producing false green
- [x] 10.93 Remove catalog summaries and key-person rows in both densities; retain title, status, one day-only `YYYY-MM-DD` Start date, and the same root Hero in both densities; render status with the same calm metadata typography as Start and no capsule/pill/chip container; retain event/people/source metrics only in Standard while Compact removes them and gives its remaining equal-size card body to the Hero; keep the left-aligned icon/wordmark/catalog-heading visual centers on one horizontal line by compensating transparent icon padding, and independently align the search-control top with the first card-row top, without changing the approved Databank typography or shell
- [x] 10.94 Replace recursive child-detail navigation with an interactive Matter-only multi-depth Sub-matters graph with primary containment and typed Matter relations, pan/zoom/reset/minimap/keyboard/bounded-continuation behavior, no per-node collapse control, and exactly one reusable two-region node quick-view overlay whose first region includes human summary/current state plus itemized facts/events/work/waits and whose second region contains only node-specific sources; it never opens another Matter reader, graph, Hero, or modal
- [x] 10.95 Preserve contained folder, Gmail thread, Codex project/workspace, or provider as a SourceGroup column in one flat Files & information table and one flat node quick-view source list; show human label, type, privacy-safe location, day-only source time, content summary, availability, and modality without repeated group subheaders or generic relationship/processing fields while retaining the complete private canonical timestamp; make table body typography smaller than headings with bounded wrapping, line height, columns, and no viewport overflow
- [x] 10.96 Direct-replace generic Hero eligibility and prompts so only roots generate natural-light photorealistic documentary/editorial images whose Matter-specific place, equipment, objects, or activity remain recognizable without a caption; reject generic/interchangeable office or people-at-computer scenes plus abstract/illustration/3D/screenshot/text/logo styles, keep descendants `not_applicable`, and prove the presentation-only privacy boundary synthetically; real current-root retirement/regeneration remains owned by 11.12j
- [ ] 10.97 Update all eleven bundled skills, manifests, prompt/schema contracts, native validators, exact overlay resolution, and the manually rehearsed Codex daily-maintenance automation for the new storage/graph/World Model/Hero contracts without named product models, API keys, or unattended final verification
- [x] 10.98 Run focused good/bad and scale/restart tests for pointer migration, raw cleanup, SourceGroup, Matter-only graph cycles/paging, hierarchy merge/reparent, logical-event dedupe/supersession, World inference, root/child Hero boundary, summary-free density parity, node-specific quick-view single-layer behavior, source grouping, human narrative rejection, empty-supplemental non-currentness, lifecycle-state/modality separation, coverage false-green rejection, bilingual equivalence, privacy, and one-click registration-to-UI coverage
- [x] 10.99 Run the iterative live 8766 browser gate at 1880x900 and 1440x900 for `en`/`zh-CN`, Standard/Compact, root cards, simplified Overview, Matter-only deep graph without collapse buttons, node-specific quick view, Files & information typography, logically deduplicated Timeline, Images, AI supplemental information, single-line coverage control, loading/processing/empty/error/stale states; measure the visible icon/wordmark/catalog-heading center-line alignment, search/first-card-row top alignment, day-only Start projection, and plain status/date metadata parity; fix and recapture every misalignment, overlap, clipping, repetition, hidden-image, abstract-Hero, nested-reader, oversized-table, empty-current, double-capsule, or stale-response defect
- [x] 10.100 Derive each Matter Start time from the earliest parseable claimed Event time, Event record time, exact related SourceVersion authored/created/modified/sent/received/observed time, or Codex first-recorded time; retain a private-path-safe typed basis, revise earlier when newly covered evidence predates it, keep Start filters/catalog hydration identical, and reject processing/scan/registration/analysis/due/deadline/Hero fallbacks
- [x] 10.101 Persist and project the exact current Matter-state basis from lifecycle/outcome ownership, label past-gap completion as bilingual `AI historical inference`, never mislabel inferred completion as reported, deduplicate historical SourceVersions into one flat file/information row, remove internal unknown coverage vocabulary from node quick view, and render planned green, in progress red, completed blue with text/icon while keeping modality out of the lifecycle label
- [x] 10.102 Implement and prove synthetic broad-human-situation reconciliation for Japan/Australia and outbound/return sibling hierarchy under 2026 travel, one Software Development root, company-level Job Search children, and typed heating/job-search cross-domain relations with append-only merge/reparent history and exact C6 ownership; real current-catalog reconciliation remains owned by 11.12k
- [x] 10.103 Add `logical_event_key`, `supersedes_event_id`, current revision, owning Matter path, importance, and conflict alternatives; migrate current duplicate timeline groups, project one current row per logical event, and keep prior revisions on demand
- [x] 10.104 Implement and prove synthetic Matter-scoped role and typed cross-Matter relationship resolution without unsafe same-name merging or fabricated people; rebuilding the user's current licensed local/Gmail/Codex candidates remains owned by 11.12l
- [x] 10.105 Generate current human-readable bilingual root/child summaries from complete current child scope using the what/current-state/next-important-thing contract; reject internal audit wording and keep evidence modality on demand
- [x] 10.106 Make AI supplemental work automatically queue for eligible Matters, bind ResearchGuard currentness and exact scope, and return current items or an honest pending/not-applicable/unavailable disposition; zero items SHALL NOT be marked current
- [x] 10.107 Simplify Overview to Hero+narrative+state+Start, vertically center its useful content, remove source/evidence/Action/OpenLoop panels, implement node-specific fact/source quick views, and direct-replace all old recursive or multi-type visible graph routes
- [x] 10.108 Replace the two-row coverage display with one slim dot+label control and connect its drilldown to indexed SourceGroup/stage/object/owner/failure/freshness/UI-ready pages; verify 180k-object first-page latency and truthful red/blue/green semantics
- [x] 10.109 Record the superseding distribution decision: keep all Guard-family skills external and independently maintained, retain exactly eleven immutable app-local Matters skills without machine-global overlays, and document ResearchGuard as the sole external research provider
- [x] 10.110 Add A3 `matters_ai_gateway_operation` after A0-A2 as a non-product FlowGuard owner for bounded model-map/context queries and append-only feedback receipts; prove it cannot become C13 or write C1-C12 state
- [x] 10.111 Add bounded MatterService/MCP model-map, situation-context, history, pending-gap, user-observation, prediction-feedback, correction, and model-miss routes that preserve modality, freshness, privacy, and original-owner authority
- [x] 10.112 Add one distributable public `matters` Codex gateway skill/plugin, separate from the eleven internal skills, with an exact service-contract reference and no bundled Guard implementation
- [x] 10.113 Update README and plugin metadata for the external ResearchGuard dependency, retired Guard-vendoring/overlay plan, public gateway versus internal-pack distinction, and event-driven default maintenance with recurring daily execution opt-in only
- [x] 10.114 Run strict OpenSpec validation, native A3 scenario/hazard/refinement checks, focused MCP/application/plugin/skill tests, and preserve final ModelMesh/TestMesh/DPF/full regression/install/release ownership for the main maintenance freeze
- [x] 10.115 Implement the bounded `source_revision_reconciliation` owner, CLI/service facade, append-only receipt, exact duplicate retirement, typed blocked outcomes, and `analysis_required` disposition that prevents an unprocessed current SourceVersion from becoming mechanically current; add C6 scenario/hazard and focused regression evidence
- [ ] 10.116 Run source-revision reconciliation across the exact canonical private Matter set, dispatch each `analysis_required` revision through its original source/evidence/semantic/Matter owners, and prove depth, localization, supplemental-information, coverage, and UI freshness are recomputed from the admitted current revisions
- [x] 10.117 Implement the provider-read-free Gmail current-scope reconciliation owner, stable keyset page, atomic CAS coverage/history/receipt transition, same-account/current-body gates, explicit pending/ambiguous/stale outcomes, MatterService/CLI facade, and focused scale/regression evidence
- [x] 10.118 Implement a bounded provider-read-free inventory-policy rebase that reuses the exact durable occurrences, preserves user intents, reclassifies under the current policy, refreshes only affected SourceGroup/coverage owners, and exposes an explicit CLI receipt instead of weakening current-scope gates
- [x] 10.119 Implement a bounded current-Matter activity reconciliation facade that resolves exact admitted SourceVersion or current-inventory user-world time, reuses the existing C5 historical correction owner, preserves unresolved gaps, rejects processing-time fallback, and has focused CLI/model regression evidence
- [x] 10.120 Correct the Gmail current-scope maintenance-time model miss so an exact current tracked body/no-text successor remains eligible after policy rebase wrote the bound metadata inventory later; add focused regression plus C1 scenario/hazard evidence
- [x] 10.121 Make passed source-annotation replay idempotent by persisting one stable annotation-to-semantic follow-up relation, reusing that exact current result, and append-only invalidating unexecuted duplicate follow-ups without deleting historical results
- [x] 10.122 Add one exact target-bound source-revision semantic owner keyed by Matter id, registry-current SourceVersion, current admission fingerprint, and current evidence anchors; adopt the revision into the existing Matter through C6 without title similarity, root duplication, or identity replacement
- [x] 10.123 Make source-revision reconciliation retire both the old SourceVersion membership and its old-revision evidence membership only after the exact current revision and current anchors are admitted, while preserving append-only historical admissions and receipts
- [x] 10.124 Add a provider-read-free Gmail content-receipt rebase that can mint only the missing digest/length receipt from an exact current SourceVersion plus exact current evidence anchors, never from metadata alone and never by copying body text into the receipt
- [ ] 10.125 Refresh C1/C6/M0, ModelMesh, FieldLifecycle, CodeContracts, TransitionCoverage, ContractExhaustion, Model-Test Alignment, TestMesh, DPF, strict OpenSpec, and focused regressions for 10.121-10.124 before consuming the repair owners on the private store
- [x] 10.126 Add exact package-id, source-revision, and task-kind selectors to the private pending-work facade and CLI so a background capability worker receives only its explicitly assigned package set while preserving the existing bounded unfiltered page as the local operator default
- [x] 10.127 Backpropagate the same-Matter sibling-refresh Model Miss: append-only supersede a stale package after an earlier admission-fingerprint change, require exactly one exact-current successor, reuse it idempotently, and reject stale retry, weakened fingerprints, or duplicate successors in C6/M0, the ledger, and focused regressions

## 11. G10 Autonomous Private First Run and Optional Correction

- [x] 11.1 Freeze the explicit Gmail account, category/query, page/cursor, attachment, and read-only authorization; begin with a bounded canary while keeping complete-account coverage unclaimed until every required page and item is terminal
- [ ] 11.2 Progressively read the authorized Inbox, Sent, and archived non-Spam/non-Trash scope, disposition every returned item and required page, and minimize signed links, codes, booking/payment details, addresses, and transport identifiers before analysis registration
- [ ] 11.3 Run real authorized messages and attachments through source/version, anchors, agent operations, automatic original-owner decisions, Matter reconciliation/hierarchy, material clues, bilingual summaries, generated heroes, supplemental information, and the actual eight-section UI under external `MATTERS_HOME`
- [ ] 11.4 Verify the UI automatically forms useful broad root/child Matters, events, people, deadlines, waiting loops, conflicts, completion gaps, latest-clue ordering, generated heroes, and advisory supplemental information from the progressively covered real scope without requiring content approval
- [x] 11.5 Verify no repository or package path receives private runtime rows, blobs, logs, screenshots, message ids, addresses, subjects, bodies, links, codes, or receipts
- [ ] 11.6 Validate restart, source change, skill failure, retry, revocation, deletion, and correction propagation on private first-run copies or minimized derivatives
- [ ] 11.7 Validate every reachable UI control through its control-event-owner-function-update-evidence chain
- [ ] 11.8 Validate keyboard, focus, cancel, error, recovery, on-demand minimized evidence, privacy, English-default launch, persisted Chinese selection, extensible locale registration, and bilingual paths
- [x] 11.9 Run end-to-end conformance replay and current generic depth/coverage reports
- [x] 11.10 Freeze a private Gmail First-Run Report with durable cursor/item terminal-coverage accounting, an opaque private evidence handle, and only an aggregate public disposition
- [ ] 11.11 Run real ResearchGuard only if its portable frozen integration/currentness receipt is current; otherwise keep research analysis visibly pending, continue inventory/autonomous non-research/optional-correction/synthetic work toward a blocked local candidate, and block both complete AI/ResearchGuard modeling and final v0.2 complete-release
- [ ] 11.12 Expand the private first run from Gmail canaries to every authorized tracked local root and reachable supported source until inventory, ObjectCoverageLedger, freshness, depth, disposition, reconciliation/hierarchy, clue/summary, localization, generated hero, supplemental information, and UI reachability reconcile
- [x] 11.12a Add stable Gmail content offset/limit resume and prevent metadata-only page rows from regressing already extracted body evidence; prove the behavior synthetically and on the first real 20-message private page
- [x] 11.12b Add the strict private-manifest Gmail body continuation leaf: exact raw-manifest hash and <=20-member batch validation, exact minimal connector projection, current metadata-owner gate, body/SourceVersion/Evidence/Coverage-only writes, model/API independence, and resumable idempotent synthetic regression
- [x] 11.12c Add the proof-bound `no_text_body` continuation terminal: require exact empty body plus connector raw-MIME recovery proof identity, preserve metadata-only relational use, write only an explicit content-disposition owner and owned not-applicable coverage, create no fake SourceVersion/EvidenceAnchor, and prove strict rejection plus exact-retry idempotence synthetically
- [x] 11.12d Backpropagate the Gmail metadata-owner model miss: register minimal SourceVersions for current metadata-only/identity-only messages, provide exact-owner-gated terminal-page reconciliation with an exclusive cursor and <=500 batch, preserve deeper bodies, create no evidence or semantic state, and prove bounded retry/no-delta behavior synthetically
- [ ] 11.12e Run Gmail current-scope reconciliation across the exact private mismatch set in bounded pages, switch only the single-scope licensed rows, preserve all ambiguous rows as blocked, and re-run downstream content-selection/analysis coverage without provider refetch
- [ ] 11.12f Rebase all current private Gmail inventory scopes to the current tracking-policy revision without provider reads, then prove the current-scope reconciliation consumes only those refreshed C1 snapshots
- [ ] 11.12g Run current-Matter activity reconciliation across the exact private canonical set after Gmail current-scope and source-revision reconciliation, then prove every current or unresolved result is represented honestly in coverage, catalog ordering, and the UI
- [ ] 11.12h Rebase missing private Gmail content receipts from exact current digest/length/evidence proofs in bounded pages, then run current-scope reconciliation once from that same terminal episode and report current, ambiguous, pending, stale, and blocked counts without exposing message content or identifiers
- [ ] 11.12i Rehearse the frozen offline source-in-place migration against the private store with a separate restorable backup, writer quiescence, derived-equivalence/integrity/count checks, and a rollback proof while keeping physical compaction separate
- [ ] 11.12j Retire private descendant/conceptual Heroes, regenerate every eligible current private root through the frozen photorealistic contract, and visually verify Matter specificity, presentation-only privacy, and honest pending/blocked states
- [ ] 11.12k Reconcile the private current catalog into broad human situations—including the licensed travel, software-development, job-search, heating, and cross-domain structures—through exact C6 ownership with append-only merge/reparent history
- [ ] 11.12l Rebuild current private person/entity candidates, Matter-scoped roles, and typed cross-Matter relationships from licensed local/Gmail/Codex evidence without unsafe same-name merging or fabricated people
- [x] 11.13 Historical v0.2.0 installed-surface check: open the installed desktop object browser against the real private run and verify the then-current Matters, timelines, people, open loops, bilingual text, representative visuals, Standard/Compact behavior, and optional corrections without exporting private screenshots or payloads; superseded by 10.44-10.60 and the next-version installation gate

## 12. G11-G12 Generic Release, Installation, GitHub Publication, and Later Private Continuation

- [x] 12.1 Recheck the complete required-public inventory before the first Git commit
- [x] 12.2 Add package metadata, CLI entrypoint, UI/package data, build configuration, changelog, security policy, and deterministic SBOM
- [x] 12.3 Freeze `liuyingxuvka/Matters` as a public repository under the MIT License while keeping private user data outside the public inventory
- [x] 12.4 Run tracked/ignored/history/staging/link/path/secret/package/metadata scans on the candidate
- [x] 12.5 Create an isolated clean clone or archive and run synthetic tests with empty private roots and fake runners
- [x] 12.6 Verify Windows local install/start and record Linux CI as not run unless an actual Linux owner executes it
- [ ] 12.7 Refreeze source, next version, generic model/test inventory, toolchain, dependencies, verifier, package, immutable bundled layer, machine-installed inventory and managed subtype, resolved active view, portable ResearchGuard receipt/provider, Git, and asset identities after all generic hierarchy/people/timeline/summary/inference/correction/UI capability work closes; private first-run completion is a separate acceptance domain
- [ ] 12.8 Run exactly one final M0+C1-C12 model execution owner in the foreground on the refrozen candidate
- [ ] 12.9 Run exactly one separate, non-overlapping final S0-S5 skill-runtime model execution owner in the foreground on the same refrozen candidate
- [ ] 12.10 Require both final model owners to terminate current, then run exactly one final full-test owner including TM19 in the foreground
- [ ] 12.11 Build the source distribution and wheel, install the frozen local package, and verify version/CLI/UI/MCP imports
- [ ] 12.12 Verify clean-machine internal Skill Pack use without global installation, then synchronize and verify only the Matters-managed subtype inside the machine-installed layer
- [ ] 12.13 Create a new privacy-audited local Git commit without private or ignored-required omissions
- [ ] 12.14 Create the next generic release tag only when commit, package, installed version, both model-owner receipts, full-test identities, anonymous install, public inventory, and privacy identities agree; do not require or imply private-first-run completion
- [ ] 12.15 Recheck an unpacked local release artifact as an anonymous consumer without private data
- [ ] 12.16 Close the current Risk Evidence Ledger, DPF synchronization domains, OpenSpec validation, and final claim boundary
- [ ] 12.17 Verify one portable frozen ResearchGuard currentness receipt or keep the local candidate blocked; no separate SourceGuard/TraceGuard/LogicGuard fallback may close the final complete-release gate
- [ ] 12.18 Verify source, local package install, immutable bundled layer, machine-installed inventory and managed subtype, resolved active-view layer, local repository, commit, tag, and local release artifact as distinct synchronized identities
- [ ] 12.19 Build and install the frozen Windows desktop package with one application shell around the packaged Web UI, installed application/desktop shortcut and icon, loopback-only service, bounded worker, private shell profile, persisted locale/density/window state, startup/recovery surface, and clean owned-process shutdown; verify desktop/service/worker/UI/skill/version identities against source, produce a desktop-specific public release ZIP that excludes self-test and `direct_url.json` build provenance and revalidates manifest/package/executable/toolchain identities, prove a browser-development URL alone cannot satisfy this gate, and restore the prior installed application transactionally on failure
- [x] 12.20 Freeze the exact GitHub repository, visibility, initial license disposition, public inventory, commit, tag, and release notes; then push and publish generic v0.3.0 without private data or a private-first-run completion claim
- [ ] 12.21 Continue the private Gmail/local/Codex first run against the latest released 0.3.x contract, reproduce reusable defects with public-safe synthetic evidence, and publish a separately frozen patch release only when accumulated generic fixes justify it
- [ ] 12.22 Correct the public source boundary wording, relicense the complete software under MIT, advance source/plugin/SBOM/package/desktop identities to v0.3.1, and publish a separately verified release without rewriting v0.3.0

## 13. Post-v0.2 Optional Connectors

- [x] 13.1 Keep the generic Jira read adapter disabled, source-only, and excluded from every v0.2 required gate
- [ ] 13.2 Keep Gmail v0.2 strictly user-authorized and read-only; allow only bounded Codex-hosted daily read/reconcile/model maintenance through the existing grant, while mailbox mutation, unattended non-Codex background sync, and general outbound-mail automation remain separate future-change decisions
- [x] 13.3 Record Jira, Rovo, advanced audio/video/archive/database/model extractors, calendar, and additional cloud-provider connectors as separate future-change decisions

## 14. Semantic State and Hierarchy Stabilization

- [x] 14.1 Freeze the orthogonal object-kind, temporal-assertion, evidence-modality, workflow-state, and terminality contract across C5-C12
- [x] 14.2 Add current-phase inference with prerequisite, active-window, remaining-obligation, contradiction, confidence, alternatives, coverage, expiry, and revisability gates
- [x] 14.3 Add provisional historical completion that is visibly AI-inferred and cannot mechanically close ancestors, independent loops, or irreversible effects
- [x] 14.4 Tighten Matter admission to stable identity plus at least two independent dimensions; reject contradictory granularity and keep single bounded steps as WorkItems
- [x] 14.5 Preserve inference basis and terminality on WorkItems, lifecycle decisions, outcomes, projections, Situation Graph nodes, and bilingual UI
- [x] 14.6 Make C7/C9 the only state/outcome owners and prohibit bounded summaries or C12 from writing canonical state
- [x] 14.7 Converge C6 reconciliation decision and canonical application onto one production owner path
- [x] 14.8 Project admitted Matters as large graph nodes and material required WorkItems as smaller stage nodes without changing Matter counts or allowing nested stage descendants
- [x] 14.9 Add Build Week registration/preparation/submission and travel purchase/boarding/flight/arrival/cancellation/postponement counterexample regressions
- [x] 14.10 Preserve graph conflicts instead of selecting maximum certainty/confidence and retire the weak secondary forecast authority
- [x] 14.11 Recompute the minimum affected private Build Week and travel objects, then prove the corrected state/basis hierarchy on port 8766
- [ ] 14.12 Rerun C5-C12, ModelMesh, TestMesh, DPF, bilingual UI, install, MCP, privacy, Git, and release gates against one frozen source identity
- [x] 14.13 Prefer one root-owned material-stage decomposition over competing legacy first-level child-Matter boxes, count the visible stages accurately, and prove Build Week three-stage and travel five-stage graphs plus one-layer quick views in the live desktop UI
