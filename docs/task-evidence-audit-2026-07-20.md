# OpenSpec task evidence audit — 2026-07-20

> Historical snapshot: this document records the state at the time of the
> audit. Later v0.3.0 release receipts and the current OpenSpec task ledger
> supersede its open-gap list; it is retained only as append-only process
> history.

This audit is intentionally limited to repository OpenSpec, FlowGuard,
documentation, and reproducible test evidence. It does not inspect or claim
completion for private databases, a real Gmail first run, installed
automations, Figma, ResearchGuard currentness, packaging, Git, tags, or
releases.

## Newly closed tasks

| Tasks | Current reproducible evidence |
|---|---|
| 7.23 | MatterService no longer contains the retired projection/intent methods, review queue, understanding-action token, old token counter, or token migration behavior. Current CLI/HTTP/MCP/UI expose no compatibility success path; focused entrypoint/autonomy tests retain only negative guards proving the retired routes stay absent. Ordinary domain rejection and content-selection deferral remain separate current concepts. |
| 8.27 | Generated-Hero tests prove one presentation-only generated asset for eligible root/independently openable child Matters, shared by Standard/Compact/detail, separate from the Images evidence gallery, with typed pending/blocked states and no ordinary cover-edit authority. |
| 10.26 | Coverage and catalog tests prove SQL/index-backed summaries, bounded paging, current relation lookup, and visible-row hydration rather than per-request full-ledger materialization. |
| 10.44, 10.46 | The BCL retains predecessor history while marking old runtime evidence stale. Current FieldLifecycleMesh structurally covers the new and retired fields, and G4's native field-lifecycle review passes. |
| 10.48–10.50 | Reconciliation, activity, execution-profile, and maintenance-orchestration tests cover multi-signal hierarchy decisions, stable material-clue ordering and bilingual summaries, model-independent capability routing, typed bounded delegation, and rejection of named-model/application-key coupling. |
| 10.52–10.54 | Generated-Hero, object-browser, and entrypoint UI tests cover private minimized briefs/assets, bilingual alt text, invalidation/retry/safety states, exactly eight detail sections, Images-gallery separation, advisory supplemental information, and the approved sidebar spacing/font geometry. |
| 10.56–10.57 | Same-class regressions cover the declared Hero/evidence/activity/order/time/locale/density/eighth-section defects. All failures were representable in current Matters-owned FlowGuard models, so the conditional Guard-upgrade path was not entered. This is not evidence that any Guard was upgraded. |
| 10.60–10.64 | Coverage, contract-rebase, global-recall, CLI, and real-service tests prove first-gap stage reporting, owner-result synchronization and invalidation, bounded resumable rebase, whole-catalog recall beyond row 50, and model-independent Codex handoffs with no application API key. |

The focused current regression command covered 18 test modules and completed
with `165 passed`.

## Tasks deliberately left open

| Tasks | Exact current gap |
|---|---|
| 8.28 | The connected Figma Starter MCP quota remains exhausted. No Figma frame or external quota result is inferred from runtime UI evidence. |
| 10.33 | No single current matrix proves both travel and job-search families across all requested child counts/depths, hierarchy mutations, profiles, locales/densities, keyboard/focus, and exact 1880x900/1440x900 geometry. |
| 10.35, 10.58 | The final model/test/prompt/skill/private-asset inventories are not frozen. DPF remains before G0 and broader peer work is still changing the candidate. |
| 10.36 | There is no current post-artifact public-boundary closure receipt covering every listed hierarchy/search/log/screenshot/receipt surface. |
| 10.45 | The BCL contains the intended commitments and primary paths, but its current native review remains blocked with 62 findings, including the unexpected `commitment_source_surface_not_bidirectional`; therefore the ledger update is not structurally closed. |
| 10.47 | Current M0/C5/C6/C10/C11/C12/A0/A1/A2 model receipts and ModelMesh pass their bounded owners, and G4 is structurally green. However TestMesh is blocked by `test_inventory_revision_mismatch`, `leaf_matrix_cell_evidence_missing`, and `required_inventory_item_owner_missing`; executable alignment is blocked by `test_proof_artifact_missing_obligation`, and UI Flow Structure reports stale installed-runtime evidence. The full rebaseline is therefore not reattached end to end. |
| 10.51 | `src/matters/bundled_skills/matters-card-visual-curation` still exists beside `matters-generated-hero`, and current frozen FlowGuard evidence still references the predecessor. No current author-side SkillGuard supervision receipt proves direct replacement. |
| 10.55 | The live UI verifier labels 1880/1440 checks, but currently executes those checks at 1880x1120 and 1440x1000. Therefore it does not prove the task's exact 1880x900/1440x900 gate for every meaningful UI batch. |
| 10.59 | Tests define the daily-maintenance receipt contract, but no current real installed Codex schedule plus manual-rehearsal/currentness receipt was verified. |
| 10.65, 10.70 | The exact required typed Model Miss set and final rerun bundle are incomplete; TestMesh full execution, DPF/private closure, and exact live/scale closure are not current. |
| 11.2–11.4, 11.6–11.8, 11.11–11.12 | These require the real authorized private first run and/or live controls. Repository synthetic tests cannot substitute for Gmail/local-source coverage, ResearchGuard currentness, private hierarchy usefulness, or every reachable installed UI path. |
| 12.7–12.19 | Final frozen identities, foreground model owners, full-test owner, packages/install, private/public audit, commit/tag/artifact, ResearchGuard currentness, and Windows desktop installation remain downstream release work. No Git or installation operation was performed by this audit. |
| 13.2 | Gmail adapter tests prove user-scoped read-only access and zero mailbox writes, but no current real Codex-hosted daily task was verified. The in-app daemon maintenance path must also be reconciled explicitly with the prohibition on unattended non-Codex background sync before this boundary can be closed. |

## Claim boundary

Checked tasks above mean that their repository behavior or model-update
obligation has current reproducible evidence. They do not upgrade DPF,
private-first-run, installed-runtime, ResearchGuard, Figma, Git, or release
claims. Unchecked tasks remain authoritative blockers.
