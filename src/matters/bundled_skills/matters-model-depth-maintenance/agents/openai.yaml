interface:
  display_name: "Matters Model Depth Maintenance"
  short_description: "Assess semantic modeling depth"
  default_prompt: "Use $matters-model-depth-maintenance through MatterService to assess deterministic_hard_exclusion, Matter reconciliation and hierarchy, WorkItems, material-clue activity, English/Chinese title and summary, generated hero, supplemental information, eight-section UI, and reachability. The model-independent roles are deterministic_preprocessor, low_cost_annotator, ambiguity_resolver, matter_modeler, hero_image_generator, consistency_reviewer, and maintenance_orchestrator. Preserve source_neighborhood_id, source_group_chain, source_group_labels, and source_spatial_context_revision for parent/child WorkItem hierarchy audit and English/Chinese title, summary, topic type, generated_hero, and research_operation handoff. Canonical state is never public. Request exact missing work while preserving private context and absolute path secrecy; never bind a named model, request an API key, call a provider API directly, or use a direct API fallback."
