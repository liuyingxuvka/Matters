# Changelog

All notable changes to Matters are recorded in this file.

## 0.2.0 — 2026-07-19

Private local desktop candidate.

- Replaced the former review-oriented surface with an AI-autonomous object
  browser based on the frozen Databank design language.
- Added Standard and Compact card modes, automatic representative visuals,
  human-readable detail, timelines, people, files, evidence, related Matters,
  and after-the-fact corrections.
- Added durable object coverage and freshness ledgers, automatic original-owner
  dispatch, background maintenance, and exact UI-reachability accounting.
- Added exactly eleven bundled Matters AI skills and version-aware active-skill
  resolution without default global installation.
- Activated the frozen ResearchGuard 0.1.1 identity with LogicGuard,
  SourceGuard, and TraceGuard as its sole members.
- Added the Windows desktop launcher, local installer, installed-runtime
  currentness checks, and private first-run boundary.
- Added resumable large-root partition traversal with bounded atomic manifest
  checkpoints, batch coverage-ledger updates, and same-class crash recovery so
  a real first run does not degrade into quadratic checkpoint writes.
- Bound partition manifests to tracking-policy revision 3 and prune known
  software-control, dependency, cache, build, test, and temporary directories
  before descendant partition work is created.
- Scan ordinary child subtrees within the frozen entry budget and create deeper
  partition nodes only after a real budget exhaustion, preventing manifest-node
  explosion without weakening inventory coverage.
- Place the installed private runtime under one non-virtualized user-profile
  directory so Python manifests and native SQLite state cannot split across
  Windows Store LocalAppData aliases.
- Select the smallest current tracked partition that can fill a bounded content
  canary, avoiding both arbitrary giant subtrees and many single-item scope
  reconciliations before a few samples can be read.
- Batch source, evidence, analysis, and projection stage updates so a bounded
  canary refreshes the global coverage summary once instead of once per item
  stage.
- Extend the same bounded aggregation rule through multi-finding original-owner
  dispatch so one AI result cannot time out after partially updating a large
  private coverage ledger.
- Add a transactionally maintained private Matter-to-coverage index so catalog
  and detail views batch relation lookups instead of reparsing the full ledger
  once per visible card.
- Prioritize connector-supplied authorized Gmail bodies inside an exact content
  budget so opaque message-id ordering cannot turn a real body canary into a
  false metadata-only result.
- Bound every installed-UI verification phase, guarantee browser-process
  cleanup on failure, and bind the resulting receipt to the exact UI authority
  revision consumed by FlowGuard.
- Extend DevelopmentProcessFlow through a privacy-minimized real-private
  aggregate, installed UI, clean-clone/package boundary, and frozen local
  Git/tag/package/install/model/TestMesh/ResearchGuard identities without
  claiming complete private semantic coverage or remote publication.
- Distinguish routine and release TestMesh parent receipts so a successful
  TM01-TM23 run, including TM19, becomes release-green and remains consumable
  by G8 without falsely claiming that the release-only suite was deferred.

## 0.1.0 — 2026-07-18

Private local release candidate.

- Added the model-first Matter pipeline with explicit source, evidence,
  lifecycle, open-loop, outcome, correction, and projection ownership.
- Added durable local inventory, freshness, background work, recomputation,
  and human-review flows.
- Added read-only local-file, document, image, Gmail, attachment, and declared
  cloud-placeholder source adapters.
- Added an English-default, Chinese-selectable UI and matching CLI, HTTP, and
  MCP locale surfaces. User-visible AI values are revision-bound locale maps.
- Added the immutable app-local Matters Skill Pack and compatibility-aware
  active skill resolution.
- Added a single abstract ResearchOperation boundary and one portable frozen
  ResearchGuard currentness receipt. Runtime startup rechecks the installed
  Python package, sole console, four consumer skill projections, manifest,
  native validation, and retired-skill residual state before enabling it.
- Added private/public filesystem separation and release-boundary checks.

This candidate is for local use. Public licensing, a public remote, and remote
release publication require a separate explicit decision.
